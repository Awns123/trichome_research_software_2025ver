# 알려진 한계

## 먼저 읽어 주세요

`src/`는 2025년 연구 당시 사용한 최종 코드의 보존본입니다. 아래 내용은 **2026년 공개 준비 과정의 사후 감사**에서 확인한 한계이며, 2025년에 이미 모두 발견하거나 수정했다는 뜻이 아닙니다. 원본 보존을 위해 `src/`는 수정하지 않았습니다.

이 코드는 반자동 분할–형태지표 추출–탐색적 통계·PCA·군집을 연결한 연구용 프로토타입입니다. 검증된 일반 목적 분석 패키지나 독립적인 생물학적 결론의 근거로 제공하지 않습니다.

## 1. 형태지표 추출: `trichome_pipeline.py`

### 연결요소 불일치

`area_px`는 mask의 모든 전경 화소로 계산하지만, 둘레·타원·convex hull은 `contours[0]`만 사용합니다. 코드 주석과 달리 가장 큰 contour를 선택하지 않습니다. 잡음이나 분리된 조각이 있으면 서로 다른 대상을 한 행의 지표에 섞을 수 있습니다.

2026년 사후 감사에서는 역사적 결과 492행 중 164행이 `P² ≥ 4πA` 조건을 위반했고, 147행의 contraction ratio가 음수였습니다. 따라서 관련 파생값은 원 연결요소를 통일해 재계산하기 전에는 정량 결론에 사용하면 안 됩니다.

### 중심선·폭·곡률

- `width_base_um`과 `width_tip_um`은 생물학적 기부·말단이 아니라, 방향이 고정되지 않은 경로의 처음·마지막 10%입니다. 실제 의미는 path-start/path-end width에 가깝습니다.
- 최장 endpoint pair 선택은 대각선과 직교 이동을 같은 한 단계로 세는 비가중 탐색을 사용합니다. 다만 선택된 경로의 최종 `length_um`은 `np.hypot`으로 대각 이동을 반영합니다.
- `skeleton_length_um`은 전체 skeleton 화소 수×pixel size이며, 기하학적 길이와 같지 않고 모든 가지를 포함합니다.
- `num_branchpoints`는 위상학적으로 고유한 분기 노드 수가 아니라 주변 이웃이 많은 skeleton 화소 수여서 한 분기부를 여러 번 셀 수 있습니다.
- 곡률값은 중심선을 평활화·등간격 재표본화하지 않은 채 연속 화소의 회전각을 요약합니다. 고전적 곡률(1/길이)이 아니며 해상도와 화소 계단 현상에 민감합니다.

### 분할·추적성

- Otsu 경로는 밝은 전경(`gray >= threshold`)을 전제합니다.
- 일부 지표는 원 mask에서, 중심선·폭은 morphology opening/closing 후 mask에서 계산되어 입력 형상이 달라질 수 있습니다.
- 외부 mask와 원영상의 크기, 자동 분할 임계값, 수동 수정 이력, 이미지·mask 해시, 이미지별 calibration 설정을 결과표에 완전히 기록하지 않습니다.
- 동일 바이트 이미지를 MD5로 건너뛰므로, 다른 폴더의 의도적 반복자료도 제외될 수 있습니다.
- morphotype의 임계값 2.0·4.0·0.4는 별도의 검증자료 없이 설정한 경험적 규칙입니다.

## 2. PCA·Ward 분석: `advanced_trichome_analyzer.py`

- 열 이름에 `id`가 들어가면 제외하는 조건 때문에 `instance_id`뿐 아니라 `width_base_um`, `width_tip_um`, `width_max_um`도 의도치 않게 빠집니다.
- 숫자형 결측은 전체 자료의 열 평균으로 대체하지만, 최종 feature 목록·대치값·scaler 값을 별도 파일에 저장하지 않습니다.
- 로그변환은 구현되어 있지 않습니다. 보고서의 분석 설명과 일치하는지 별도로 대조해야 합니다.
- `--mode individual`은 개체 수준 덴드로그램을 만들지 않고 PCA까지만 실행합니다. Ward 군집은 `species` 모드에서만 수행합니다.
- feature 열을 bootstrap 단위로 취급하지만 파생·상관 형질의 비독립성을 반영하지 않으며 random seed가 없습니다.
- `pca_results.csv`는 원 이미지와 `instance_id`를 보존하지 않아 PCA 점을 원 측정물로 완전히 역추적할 수 없습니다.
- 이른바 family PCA는 과마다 PCA를 새로 적합한 것이 아니라 전체 PCA 좌표를 과별로 필터링한 그림입니다.
- zoomed 그림은 전역 2.5×IQR 밖의 점을 보이지 않지만 PCA 계산에는 포함하며, 숨겨진 점 목록을 저장하지 않습니다.
- 대화형 과 매핑을 설정 파일로 저장하지 않고 `plt.show()`를 호출하므로 무인 재실행이 불편합니다.

Ward/Newick 출력은 형태 유사도 군집도이지 진화계통수가 아닙니다.

## 3. 탐색적 통계 도구 3종

대상 파일은 `merge_and_analyze.py`, `normality_validation.py`, `advanced_statistical_analysis.py`입니다.

- 모든 숫자형 열을 형질로 선택하므로 `instance_id` 같은 식별자도 검정 대상이 됩니다.
- 같은 이미지·잎·식물 개체에서 나온 여러 모용을 독립 관측치로 취급할 수 있습니다. 계층 식별자가 없어서 유사반복을 보정하지 못합니다.
- 초기 ANOVA 도구는 정규성·등분산·독립성을 먼저 검증하지 않습니다.
- Shapiro-Wilk의 `p > 0.05`를 정규성 확인으로 과도하게 해석할 수 있고, 표본 수가 3보다 작은 집단은 건너뜁니다.
- `normality_validation.py`는 모든 수치형 열에 `kde=True`를 적용하므로, 어떤 종에서 값이 모두 같은 형질은 singular covariance 오류로 PDF 생성을 중단시킬 수 있습니다.
- 로그변환 뒤 정규성과 등분산성을 다시 확인하지 않으며, ANOVA와 Kruskal-Wallis 중 주 판단 기준을 사전 고정하지 않았습니다.
- 여러 형질에 대한 다중검정 보정, 효과크기, 신뢰구간, Welch ANOVA와 Kruskal-Wallis 사후검정이 없습니다.
- `ellipse_angle_deg`처럼 원형통계가 필요한 값도 일반 선형 변수처럼 처리합니다.

따라서 이 도구들은 “통계 검증 완료”의 증거가 아니라, ANOVA에서 출발해 분포 진단·변환·비모수 검정으로 분석 질문을 확장한 탐색 프로그램으로 봐야 합니다.

## 4. 이진 성분 군집: `component_tree_builder_v5.py`

- 산출물은 SMC/Jaccard 기반 성분 유사도 UPGMA 군집도이며 진화계통수가 아닙니다.
- 입력을 정수로 변환하지만 0/1 이진성, 결측, 중복 종명과 변이 없는 형질을 충분히 검증하지 않습니다.
- SMC는 공동 부재를 유사성으로 계산해 희소 이진자료에서 유사도를 부풀릴 수 있습니다.
- 역사적 입력에는 전체 성분 존재와 같은 성분의 구간값이 함께 있어 일부 성분이 중복 가중될 수 있습니다.
- 서로 독립이 아닌 구간 형질을 열 단위 bootstrap으로 재표집하고 random seed가 없습니다.
- 정의되지 않은 거리를 숫자로 치환하거나 실패 반복을 분모에서 제외해 support를 과대평가할 수 있습니다.
- 대화형 과 매핑을 저장하지 않고, 표본 수에 따라 Newick 가지 길이 산출 규칙이 일관되지 않습니다.
- 이 V5는 SMC와 Jaccard만 구현합니다. 과거 폴더에 남은 Dice 결과를 이 파일의 산출물로 제시하지 않습니다.

## 5. 공통 재현성 한계

2025년 원 코드에는 정확한 Python·패키지 버전, random seed, 입력자료 해시, 실행 명령·설정, 제외행 로그가 완전하게 고정되어 있지 않습니다. 현재 README와 합성 예제를 추가해도 2025년 당시 완전 재현 가능했다고 소급해 말하지 않습니다.

실제 연구자료를 공개하지 않았으므로, 이 저장소가 제공하는 범위는 코드 구조·실행 인터페이스·합성자료 실행 확인까지입니다.

일부 CP949 기반 Windows 콘솔에서는 도움말·진행문에 포함된 `µ` 문자를 출력할 때 `UnicodeEncodeError`가 날 수 있습니다. 공개본 검증에서는 `PYTHONUTF8=1`로 실행했습니다.
