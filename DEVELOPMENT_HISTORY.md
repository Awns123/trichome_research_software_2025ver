# 개발 과정 재구성

이 문서는 2025년 연구 아카이브에 남은 코드·산출물을 2026년 공개 준비 과정에서 대조해 재구성한 개발 흐름입니다. Git 기록이 아니며, 각 단계가 완전히 선형적으로 개발되었다거나 아래의 모든 한계를 당시 이미 인식했다는 뜻은 아닙니다. 중간 코드는 원 아카이브에 보존하고 이 공개본에는 최종 여섯 파일만 넣었습니다.

## 1. 이미지에서 형태지표까지

현재 아카이브에는 `trichome_pipeline.py`의 SHA-256 고유 코드가 한 상태만 남아 있어 날짜별 버전 변화를 주장할 수 없습니다. 다만 최종 파일에는 다음 기능이 함께 구현되어 있습니다.

1. 수동 다각형 주석
2. JPEG·PNG·다중 페이지 TIFF 입력
3. 스케일바 두 점 클릭을 통한 화소 크기 보정
4. instance mask 생성·재사용
5. 골격화와 endpoint/branch 후보 탐색
6. 중심선·폭·곡률·기본 형상 지표 추출
7. flood-fill 자동 주석
8. polygon ROI 안의 Otsu·GrabCut 분할
9. tip/root 클릭을 사용한 dual-curve 경계 보정 시도
10. 재귀 폴더 처리, 이미지 해시 중복 억제, QC·통합 CSV 저장

따라서 이 파일은 “여러 버전이 모두 남은 파이프라인”이 아니라, 연구 작업을 한 흐름으로 연결하려 한 **최종 통합 프로토타입 한 개**로 설명합니다.

## 2. 형태 PCA·Ward 분석

| 단계 | 주요 변화 | 현재 판단 |
|---|---|---|
| V0 | CSV 병합, 표준화, PCA, Ward 군집의 기본 흐름 | 출발점 |
| V1 | 식별 열 제외, 데이터 점검, 산출물 확대 | 실질적 개선 |
| V1-BW | 흑백 발표용 시각화 | 알고리즘 개선이 아닌 표현 분기 |
| V2 | 과 매핑과 과별 PCA 그림 | 기능 확장과 변수 선택 퇴보가 함께 발생 |
| V3 | 과별 dendrogram과 Newick 저장 | 기능 확장 |
| V4 | scree, loading, feature bootstrap | 탐색 범위 확장; seed·독립성 한계 잔존 |
| V4-log | 로그변환을 넣으려 한 부분 패치 | 불완전·실행 불가, 최종본에서 제외 |
| V5 | PCA 좌표·설명력 저장, global/zoom/biplot, 범례 분리 | 결과 기록성·시각화 개선, 핵심 PCA·Ward 방법은 V4와 동일 |

공개한 `advanced_trichome_analyzer.py`는 V5입니다. V5의 강점은 새로운 분류 알고리즘을 완성했다는 데 있지 않고, 계산 결과를 파일로 남기고 서로 다른 범위의 그림을 비교 가능하게 만든 데 있습니다. 한편 `width_*` 열의 의도치 않은 제외, unseeded bootstrap과 추적성 문제는 최종본에도 남아 있습니다.

## 3. 통계 질문의 확장

2025-10-07 아카이브에는 약 1시간 20분 사이에 만들어진 세 도구가 남아 있습니다.

| 시각(KST) | 파일 | 질문의 변화 |
|---|---|---|
| 15:44 | `merge_and_analyze.py` | 종별 CSV를 합치고 ANOVA·Tukey를 자동화 |
| 16:37 | `advanced_statistical_analysis.py` | 정규성 문제를 확인하고 변환 ANOVA와 Kruskal-Wallis를 함께 탐색 |
| 17:02 | `normality_validation.py` | 종×형질별 Shapiro 결과와 분포 그림을 별도 기록 |

이 흐름은 “통계 검증을 완성했다”는 뜻이 아닙니다. 초기 ANOVA의 가정 문제를 의식해 진단과 비모수 방법을 추가한 학습 과정이며, 유사반복·다중검정·효과크기·사전 판단 기준 문제는 남았습니다.

## 4. 이진 성분 유사도 군집의 시도와 롤백

| 단계 | 주요 변화 | 현재 판단 |
|---|---|---|
| C1 | SMC·Jaccard 거리, UPGMA, feature bootstrap | 기본 작동본 |
| C2 | Dice와 Neighbor-Joining 추가 시도 | NJ 거리행렬 형식 오류로 완성되지 않음 |
| C3 | 거리행렬 변환 함수 분리 | condensed distance 매핑 오류가 추가된 퇴보 |
| C4/V5 | 실패한 NJ 제거, SMC·Jaccard UPGMA와 전체·과별 분석으로 축소 | 작동하는 범위로 되돌린 합리적 롤백 |

공개한 `component_tree_builder_v5.py`는 C4/V5입니다. 이 파일에는 Dice와 NJ가 없습니다. 이전 폴더에 남아 있던 Dice 산출물을 V5 결과라고 주장하지 않으며, 산출물을 진화계통수가 아닌 성분 유사도 군집도로 부릅니다.

## 개발 과정에서 남은 핵심

```text
정성 관찰의 한계
  → 이미지 정량화
  → 입력부터 CSV까지 자동화
  → 변수 선택·분포 가정 점검
  → PCA·군집의 결과 기록 확대
  → 실패한 확장을 제거하고 작동 범위로 롤백
  → 2026년 사후 감사에서 오류와 주장 범위를 다시 명시
```

강점은 파일 수나 코드 길이가 아니라, 실제 연구 질문을 계산 가능한 흐름으로 바꾸고 실패·퇴보·롤백까지 자료로 남긴 과정입니다.

